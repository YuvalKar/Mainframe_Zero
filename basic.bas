10 REM *** NBAYA FIRST RUN ***
20 LET A = 3
30 PRINT "CHECKING VARIABLE A."
40 IF A = 5 THEN GOTO 999
50 PRINT "A IS NOT 5 - LOOP"
60 GOTO 30
999 PRINT "EXITING TO SYSTEM"
1000 END


CREATE A PICTURE:
Single line of glowing, monospaced retro 80s computer 
green text on a solid black background, 
simulating a CRT monitor screen. 
Image resolution is exactly 512x512 pixels. 
Include scanlines, and the exact text: 'RUN'
Do not include any other text or elements.